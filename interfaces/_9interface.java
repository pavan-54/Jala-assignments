package interfaces;

public interface _9interface {
		/*
		 * the access modifiers that are avialable to interfaces are only public and abstract
		 * no other cannot be use for an interface
		 */

}
