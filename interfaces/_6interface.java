package interfaces;

public interface _6interface {

	/*
	 * We can define the default methods in the interface itself
	 */
	default void meth() {
		System.out.println("This is a default method");
	}

}
